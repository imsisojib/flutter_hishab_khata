import 'package:flutter/material.dart';

class ScreenCustomerUpdate extends StatelessWidget{
  const ScreenCustomerUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}