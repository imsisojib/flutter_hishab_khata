import 'package:flutter/material.dart';

class ScreenOrderUpdate extends StatelessWidget{
  const ScreenOrderUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}